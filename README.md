# RideShare-Application
This project implements a fault-tolerant, highly available Database-as-a-Service (DBaaS) for the RideShare application. The Users and Rides microservices route all database requests through a central orchestrator instead of calling the DB APIs on localhost.
